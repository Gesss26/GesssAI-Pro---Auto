<<<<<<< HEAD
name: Scrape Football Data

on:
  schedule:
    # Esegui ogni giorno alle 6:00 UTC (9:00 Italia)
    - cron: '0 6 * * *'
  # Permetti esecuzione manuale
  workflow_dispatch:
=======
name: Scrape Calendari Calcio

on:
  schedule:
    # Esegue ogni giorno alle 6:00 (UTC)
    - cron: '0 6 * * *'
  workflow_dispatch:
    # Permette l'esecuzione manuale

permissions:
  contents: write
>>>>>>> f631f8251f6dc92470e440f0ddec2d30e80f1606

jobs:
  scrape:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
<<<<<<< HEAD
      
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas beautifulsoup4 requests openpyxl
        
    - name: Run scraper
      run: |
        python scarica.py
        
    - name: Commit and push if changed
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add data/matches.json excel/GesssAI_Input.xlsx excel/GesssAI_Input.json || true
        git diff --staged --quiet || git commit -m "Aggiornamento dati $(date +'%Y-%m-%d %H:%M')"
        git push
=======
      with:
        token: ${{ secrets.GESSSAI_PRO_AUTO }}
        fetch-depth: 0
    
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests pandas beautifulsoup4 openpyxl
    
    - name: Create directories
      run: |
        mkdir -p excel data
    
    - name: Run scraper
      run: |
        # Rimuove la riga dell'input() che causa problemi in CI
        cp scarica.py scarica_original.py
        sed -i '/input("🔴 Premere INVIO per uscire...")/d' scarica.py
        python scarica.py
    
    - name: Check if matches.json exists
      id: check_json
      run: |
        if [ -f "data/matches.json" ]; then
          echo "exists=true" >> $GITHUB_OUTPUT
          echo "✅ matches.json trovato"
          echo "📊 Dimensione: $(du -h data/matches.json | cut -f1)"
          if command -v jq &> /dev/null; then
            echo "📊 Numero partite: $(jq '.matches | length' data/matches.json)"
            echo "🏆 Campionati: $(jq '.championships | length' data/matches.json)"
          fi
        else
          echo "exists=false" >> $GITHUB_OUTPUT
          echo "❌ matches.json non trovato"
        fi
    
    - name: Commit and push matches.json
      if: steps.check_json.outputs.exists == 'true'
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add data/matches.json
        if git diff --staged --quiet; then
          echo "📝 Nessuna modifica da committare"
        else
          git commit -m "Aggiornamento automatico matches.json $(date +'%Y-%m-%d %H:%M')"
          git push
          echo "✅ Push effettuato con successo"
        fi
      env:
        GITHUB_TOKEN: ${{ secrets.GESSSAI_PRO_AUTO }}
    
    - name: Upload artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: scrape-data
        path: |
          excel/
          data/
        retention-days: 7
    
    - name: Summary
      if: always()
      run: |
        echo "========================================"
        echo "✅ Scraping completato!"
        echo "📅 Data: $(date)"
        if [ -f "data/matches.json" ]; then
          echo "📊 matches.json generato con successo"
          echo "📁 Dimensione: $(du -h data/matches.json | cut -f1)"
        else
          echo "❌ matches.json NON generato"
        fi
        echo "========================================"
>>>>>>> f631f8251f6dc92470e440f0ddec2d30e80f1606
